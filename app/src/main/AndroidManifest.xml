<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edd.proyecto1_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".menuPrincipalActivity"
            android:label="@string/MenuName" />
        <activity
            android:name=".MainActivity"
            android:label="@string/LoginApp_name"
            android:theme="@style/AppTheme">
           <!-- <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity
            android:name=".RentaActivity"
            android:label="@string/RentaName"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DevolucionesActivity"
            android:label="@string/DevName"
            android:theme="@style/AppTheme" />
        <activity android:name=".RentaActivos"
            android:theme="@style/FullscreenTheme"
            android:label="@string/app_name"

            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>